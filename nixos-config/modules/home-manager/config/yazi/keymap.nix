{ config, pkgs, ... }:

{
  # User needs to provide their desired Yazi keybindings here.
  # Example:
  # programs.yazi.keymap = {
  #   manager.prepend_keymap = [
  #     { on = [ "j" ]; run = "move_down"; }
  #     { on = [ "k" ]; run = "move_up"; }
  #   ];
  # };
}
